# 本项目完全离线运行。请在首次正式使用前修改管理员密码。
ADMIN_USERNAME=admin
ADMIN_PASSWORD=insight-admin

# 可选：数据文件存储目录，默认是项目目录下的 data/
# DDT_DATA_DIR=/absolute/path/to/data

# 本地 HTTP 部署保持 false；只有在 HTTPS 后方部署时才设为 true。
COOKIE_SECURE=false

# 单个表格上传上限与 Docker Compose 对外端口。
MAX_IMPORT_MB=200
DDT_PORT=3000
